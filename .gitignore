# E2E 스크린샷 (테스트 실행 시 자동 생성)
e2e/screenshots/*.png
e2e/screenshots/*.jpg

# Dependencies
node_modules/
.pnp
.pnp.js

# Expo
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
*.apk
*.aab
*.ipa

# Native
ios/Pods/
android/build/
android/app/build/
android/.gradle/

# Build artifacts
build/
*.xcarchive

# Fastlane outputs
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output
fastlane/README.md

# Metro
.metro-health-check*

# Env — 모든 .env 변형 ignore. placeholder (.env.example) 만 예외.
.env
.env.*
!.env.example
!.env.sample

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Testing
coverage/

# Supabase
supabase/.branches
supabase/.temp

# Claude Code project files (agent context, not source code)
.claude/

# Project documentation (agent handoffs, tasks, issues — not shipped)
docs/

# Backup files
*.bak

# E2E / QA screenshot artifacts (regenerated each run, not source of truth)
screenshots/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Local credential store — see credentials/README.md (Sprint 19)
credentials/

# Native iOS / Android dirs — regenerated by `npx expo prebuild` (Sprint 19).
# Source of truth lives in app.json + plugins/. EAS Build also auto-runs
# prebuild when these are absent, so committing them would just create
# drift. Reuse cached prebuild via .expo/prebuild/ if EAS suggests it.
ios/
android/
# Firebase config (민감 — Firebase Console 에서 재다운로드 가능)
GoogleService-Info.plist
google-services.json

# 기타 민감 정보 — 흔히 빠지는 패턴 보강
*.cer
*.crt
*.pfx
service-account*.json
*-credentials.json
auth-token*
AuthKey_*
.npmrc

# 스토어 스크린샷 합성 중간물 — 최종본은 images/store/ 에 커밋된다.
# shots/(원본 캡처)는 추적한다: 없으면 브랜드 문구만 고칠 때도 dev 웹을 띄워
# 전체 재촬영을 해야 한다(2026-08 리브랜딩 때 실제로 그랬다).
scripts/store-screenshots/out/

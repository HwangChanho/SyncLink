# .easignore — controls which files EAS Build uploads (used INSTEAD of .gitignore).
#
# Mirror of .gitignore, EXCEPT it does NOT ignore google-services.json:
# that file is gitignored (kept out of the PUBLIC repo for hygiene) but the
# Android build needs it (Google Sign-In + FCM via expo-notifications). This
# lets EAS upload it for the build without committing it to git.
# Keep this in sync with .gitignore when adding new secret-exclusion patterns.

# E2E 스크린샷 (테스트 실행 시 자동 생성)
e2e/screenshots/*.png
e2e/screenshots/*.jpg

# Dependencies
node_modules/
.pnp
.pnp.js

# Expo
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
*.apk
*.aab
*.ipa

# Native
ios/Pods/
android/build/
android/app/build/
android/.gradle/

# Build artifacts
build/
*.xcarchive

# Fastlane outputs
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output
fastlane/README.md

# Metro
.metro-health-check*

# Env — 모든 .env 변형 ignore.
.env
.env.*
!.env.example
!.env.sample

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Testing
coverage/

# Supabase
supabase/.branches
supabase/.temp

# Claude Code project files
.claude/

# Project documentation (not shipped)
docs/

# Backup files
*.bak

# E2E / QA screenshot artifacts
screenshots/

expo-env.d.ts

# Local credential store
credentials/

# Native iOS / Android dirs — regenerated by expo prebuild
ios/
android/

# Firebase config — GoogleService-Info.plist stays excluded (iOS uses a
# different path); google-services.json is intentionally NOT listed here so
# EAS uploads it for the Android build.
GoogleService-Info.plist

# 기타 민감 정보
*.cer
*.crt
*.pfx
service-account*.json
*-credentials.json
auth-token*
AuthKey_*
.npmrc
